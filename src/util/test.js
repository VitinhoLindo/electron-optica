// let test = {
//   text: 'Teste',
//   bufferText: null,
//   base64TextEncoded: null,
//   hexadecimalTextEncoded: null,
//   base64TextDecoded: null,
//   hexadecimalTextDecoded: null,
//   textBufferValues: []
// }

// test.bufferText = this.$app.stringToBuffer(test.text)
// test.base64TextEncoded = this.$app.bufferTo(test.bufferText, 'base64')
// test.hexadecimalTextEncoded = this.$app.bufferTo(test.bufferText, 'hex')
// test.base64TextDecoded = this.$app.toBuffer(test.base64TextEncoded.value, 'base64')
// test.hexadecimalTextDecoded = this.$app.toBuffer(test.hexadecimalTextEncoded.value, 'hex')

// test.textBufferValues.push(
//   this.$app.bufferToString(test.bufferText),
//   this.$app.bufferToString(test.base64TextDecoded),
//   this.$app.bufferToString(test.hexadecimalTextDecoded)
// )